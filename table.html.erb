<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Coq bench</title>

    <!-- Bootstrap -->
    <link href="../../bootstrap.min.css" rel="stylesheet">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <div class="container">
      <div class="navbar navbar-default" role="navigation">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand" href="../..">Coq bench</a>
          </div>
        </div>
      </div>
      <div class="article">
        <div class="row">
          <div class="col-md-12">
            <h1><%= h(coq_version) %></h1>
            <% os, machine, ocaml, opam = architecture.split("-") %>
            <em><%= h("#{os}, #{machine}, OCaml #{ocaml}, Opam #{opam}") %></em>
            <% for repository in [:stable, :testing, :unstable] %>
            <h2><%= h(repository.to_s.capitalize) %> repository</h2>
            <table class="table table-striped text-center">
              <tbody>
                <% for name, versions in database.packages(architecture, coq_version, repository) %>
                <tr>
                  <td><%= h(name) %></td>
                  <% for version in versions %>
                    <%
                      bench = database.read_history(architecture, coq_version, repository, name, version)[0]
                      raise "csv file empty for #{architecture}, #{coq_version}, #{repository}, #{name}, #{version}" if bench.nil?
                    %>
                  <td class="<%= css_class_of_bench(bench) %>"><a href="<%= "#{u(repository)}/#{u(name)}/#{u(version)}.html" %>"><%= h(version) %></a></td>
                  <% end %>
                </tr>
                <% end %>
              </tbody>
            </table>
            <% end %>
            <ul class="list-inline">
              <li>Colors:</li>
              <li class="bg-success">Success</li>
              <li class="bg-info">Not compatible</li>
              <li class="bg-warning">Error in dependencies</li>
              <li class="bg-danger">Error</li>
            </ul>
          </div>
        </div>
      </div>
      <hr/>
      <div class="footer">
        <p class="text-center">
          <small>Project <a href="https://github.com/coq-bench">coq-bench</a>. © Guillaume Claret.</small>
        </p>
      </div>
    </div>

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="../../bootstrap.min.js"></script>
  </body>
</html>